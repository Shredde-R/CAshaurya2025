# Backend environment variables
# Copy to .env and fill in values

# Python path — ensures root-level modules (lstm.py etc.) are importable
# Set this when running locally: $env:PYTHONPATH = "."
# On Render this is set automatically via render.yaml
PYTHONPATH=.
